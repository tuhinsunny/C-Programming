#include<stdio.h>
#include<string.h>

int main() {
    // char oldStr[100] = "oldStr";
    // char newStr[] = "newStr";
    // strcat(oldStr,newStr);
    // // strcpy(newStr , oldStr);
    // // puts(newStr);  
    // puts(oldStr);

    char str[] = "Banana";
    char st[]= "Banana";
    printf("%d",strcmp(str,st));
    return 0;
}